/**
 * Flags landing marketing — social proof à activer quand les chiffres sont vérifiables en prod.
 */

/** Bandeau « Studios actifs en France » + villes défilantes (LandingHeroMarquee). */
export const LANDING_HERO_STUDIO_MARQUEE_ENABLED = false;

/**
 * Embed Testimonial.to — mur public https://testimonial.to/inkflow/all
 * contient au moins un avis validé (sinon l’iframe affiche « No testimonials found » en anglais).
 */
export const LANDING_TESTIMONIAL_TO_EMBED_ENABLED = false;
